.. _panos_http_profile_module:


panos_http_profile -- Manage http server profiles.
==================================================

.. contents::
   :local:
   :depth: 1


Synopsis
--------

Manages http server profiles.




Requirements
------------
The below requirements are needed on the host that executes this module.

- pan-python
- pandevice >= 0.11.1
- PAN-OS >= 8.0



Parameters
----------

  name (required, type: str)
    Name of the profile.


  tag_registration (type: bool)
    The server should have user-ID agent running in order for tag registration to work.


  config_name (type: str)
    Name for custom config format.


  config_uri_format (type: str)
    URI format for custom config format.


  config_payload (type: str)
    Payload for custom config format.


  system_name (type: str)
    Name for custom config format.


  system_uri_format (type: str)
    URI format for custom config format.


  system_payload (type: str)
    Payload for custom config format.


  threat_name (type: str)
    Name for custom config format.


  threat_uri_format (type: str)
    URI format for custom config format.


  threat_payload (type: str)
    Payload for custom config format.


  traffic_name (type: str)
    Name for custom config format.


  traffic_uri_format (type: str)
    URI format for custom config format.


  traffic_payload (type: str)
    Payload for custom config format.


  hip_match_name (type: str)
    Name for custom config format.


  hip_match_uri_format (type: str)
    URI format for custom config format.


  hip_match_payload (type: str)
    Payload for custom config format.


  url_name (type: str)
    Name for custom config format.


  url_uri_format (type: str)
    URI format for custom config format.


  url_payload (type: str)
    Payload for custom config format.


  data_name (type: str)
    Name for custom config format.


  data_uri_format (type: str)
    URI format for custom config format.


  data_payload (type: str)
    Payload for custom config format.


  wildfire_name (type: str)
    Name for custom config format.


  wildfire_uri_format (type: str)
    URI format for custom config format.


  wildfire_payload (type: str)
    Payload for custom config format.


  tunnel_name (type: str)
    Name for custom config format.


  tunnel_uri_format (type: str)
    URI format for custom config format.


  tunnel_payload (type: str)
    Payload for custom config format.


  user_id_name (type: str)
    Name for custom config format.


  user_id_uri_format (type: str)
    URI format for custom config format.


  user_id_payload (type: str)
    Payload for custom config format.


  gtp_name (type: str)
    Name for custom config format.


  gtp_uri_format (type: str)
    URI format for custom config format.


  gtp_payload (type: str)
    Payload for custom config format.


  auth_name (type: str)
    Name for custom config format.


  auth_uri_format (type: str)
    URI format for custom config format.


  auth_payload (type: str)
    Payload for custom config format.


  sctp_name (type: str)
    PAN-OS 8.1+.

    Name for custom config format.


  sctp_uri_format (type: str)
    PAN-OS 8.1+.

    URI format for custom config format.


  sctp_payload (type: str)
    PAN-OS 8.1+.

    Payload for custom config format.


  iptag_name (type: str)
    PAN-OS 9.0+.

    Name for custom config format.


  iptag_uri_format (type: str)
    PAN-OS 9.0+.

    URI format for custom config format.


  iptag_payload (type: str)
    PAN-OS 9.0+.

    Payload for custom config format.


  provider (type: )
    A dict object containing connection details.


    ip_address (type: str)
      The IP address or hostname of the PAN-OS device being configured.


    username (type: str, default: admin)
      The username to use for authentication.  This is ignored if *api_key* is specified.


    password (type: str)
      The password to use for authentication.  This is ignored if *api_key* is specified.


    api_key (type: str)
      The API key to use instead of generating it using *username* / *password*.


    port (type: int, default: 443)
      The port number to connect to the PAN-OS device on.


    serial_number (type: str)
      The serial number of a firewall to use for targeted commands. If *ip_address* is not a Panorama PAN-OS device, then this param is ignored.



  ip_address (type: str)
    **Deprecated**

    Use *provider* to specify PAN-OS connectivity instead.

    ------------

    The IP address or hostname of the PAN-OS device being configured.


  username (type: str, default: admin)
    **Deprecated**

    Use *provider* to specify PAN-OS connectivity instead.

    ------------

    The username to use for authentication.  This is ignored if *api_key* is specified.


  password (type: str)
    **Deprecated**

    Use *provider* to specify PAN-OS connectivity instead.

    ------------

    The password to use for authentication.  This is ignored if *api_key* is specified.


  api_key (type: str)
    **Deprecated**

    Use *provider* to specify PAN-OS connectivity instead.

    ------------

    The API key to use instead of generating it using *username* / *password*.


  port (type: int, default: 443)
    **Deprecated**

    Use *provider* to specify PAN-OS connectivity instead.

    ------------

    The port number to connect to the PAN-OS device on.


  vsys (type: str, default: shared)
    The vsys this object belongs to.


  device_group (type: str, default: shared)
    (Panorama only) The device group the operation should target.


  state (type: str, default: present)
    The state.





Notes
-----

.. note::
   - Panorama is supported.
   - Check mode is supported.
   - PAN-OS connectivity should be specified using *provider* or the classic PAN-OS connectivity params (*ip_address*, *username*, *password*, *api_key*, and *port*).  If both are present, then the classic params are ignored.




Examples
--------

.. code-block:: yaml+jinja

    
    # Create a profile
    - name: Create http profile
      panos_http_profile:
        provider: '{{ provider }}'
        name: 'my-profile'
        tag_registration: true





Status
------




- This module is not guaranteed to have a backwards compatible interface. *[preview]*


- This module is maintained by community.



Authors
~~~~~~~

- Garfield Lee Freeman (@shinmog)

